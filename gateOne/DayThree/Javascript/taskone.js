const prompt = require("prompt-sync")();

double check = Math.random();

prompt = Number("2 * 2");

for(let count = 0; count < 12; count++)  {
     check = prompt
   }

 for(let count = 0; count < 12; count++) {
  let check = (int)(Math.random()) + number;
   }
     
   console.log(Math.floor()(Math.random() * prompt));
   

}

}